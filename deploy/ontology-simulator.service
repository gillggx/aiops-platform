[Unit]
Description=OntologySimulator MES Backend (port 8012)
After=network.target mongod.service
Wants=mongod.service

[Service]
Type=simple
User=ubuntu
Group=ubuntu
WorkingDirectory=/opt/aiops/ontology_simulator
EnvironmentFile=/opt/aiops/ontology_simulator/.env
ExecStart=/opt/aiops/venv_ontology/bin/uvicorn main:app --host 127.0.0.1 --port 8012
Restart=on-failure
RestartSec=5
StandardOutput=journal
StandardError=journal
SyslogIdentifier=ontology-simulator

NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=read-only
ReadWritePaths=/opt/aiops/ontology_simulator /tmp

[Install]
WantedBy=multi-user.target
